//! Local-model sidecar client: loopback-only probing and an OpenAI-compatible
//! chat client for the opt-in intelligence layer. This module is only ever
//! reachable from the `optimize` command; no deterministic surface imports it.
